const express = require('express');
const cors = require('cors')
const app = express();


app.use(express.json());

let paints = [1,2];


// app.post("/paint", (req,res)=>{
//     const paint = req.body;
//     paints.push(paint);
//     res.json({
//         message: "paint added",
//         data : paints
//     })
// })

    app.get('/paints',(req : Request,res : Response)=>{
        res.send(paints)
    })



app.listen(3000,()=>{
    console.log("server runiiinn")
})

